program recoveryTest;
var a, b, c : int;
    x, y : float;

void testFunc(p : int)
[ var local : int;
  
  { // Error 1: doble operador * en expresión
    local = 10 * * 5;
    
    // Statement válido después del error
    print("Después del primer error");
  }
];

void calculate(num : int)
[ var result : int;
  
  { result = num * 2;
    
    // Error 2: expresión incompleta en asignación
    result = result + ;
    
    print("Continuando después del error 2");
  }
];

main 
{ a = 5;
  b = 10;
  
  // Error 3: expresión incompleta
  c = a + ;
  
  // Se recupera y continúa
  print("Después del error 3");
  
  // Error 4: condición incompleta en if
  if (a > )
  {
    print("En if");
  };
  
  // Se recupera y continúa
  b = 20;
  print("b ahora es 20");
  
  // Error 5: error en argumentos de print
  print(a, , b);
  
  // Se recupera y continúa
  print("Después del error 5");
  
  // Error 6: error en argumentos de llamada a función
  testFunc(5, , 3);
  
  // Statement válido al final
  c = a * b;
  
  // Error 7: error en expresión dentro de paréntesis
  c = (a + );
  
  print("Fin del programa");
}
end