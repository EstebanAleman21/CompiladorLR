program testLimit;
var n : int;

void countdown(x : int)
[ var temp : int;
  { if (x > 0)
    { temp = x - 1;
      countdown(temp);
    }
    else
    { print("Done!"); };
  }
];

main
{ n = 1001;
  countdown(n);
}
end
