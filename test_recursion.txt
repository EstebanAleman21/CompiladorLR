program testRecursion;
var n : int;

void countdown(x : int)
[ var temp : int;
  { if (x > 0)
    { print(x);
      temp = x - 1;
      countdown(temp);
    }
    else
    { print("Done!"); };
  }
];

main
{ n = 1000;
  countdown(n);
}
end
